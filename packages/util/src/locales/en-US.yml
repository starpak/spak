# 包级翻译分片（util）— 权威源为项目根 /locales/en-US.yml，本文件由拆分脚本生成
# 供发布后的包自包含 i18n；加载时会与根 locales 合并（根优先）
spak:
  util:
    escape_reg_exp: Escaped regular expression string
    interpolated: 'Interpolated string: {result}'
    observed: 'Observed object: {label}'

